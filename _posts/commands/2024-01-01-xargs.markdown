---
layout: post
category: commands
title: xargs
---

## interval

1つの引数ずつコマンドを実行し、実行間隔を空ける。

- `-n1`: 1つの引数ずつコマンドを実行
- `sh -c '...' _`: `_` は `$0` を埋めるためのダミー引数(渡した引数は `$1` に入る)

```sh
printf '%s\n' a b c  | xargs -n1 sh -c 'echo ${1}; sleep 5' _
```

`a`, `b`, `c` を順に表示し、各実行間で5秒待つ。

